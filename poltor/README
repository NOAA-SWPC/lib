The following programs exist:

preproc.c     - read in satellite data, preprocess by filtering
                according to UT, kp, etc, and write a new (smaller)
                data file
invert_main.c - read input data file generated by preprocessor and
                solve LS system
